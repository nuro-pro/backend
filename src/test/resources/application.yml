spring:
  profiles:
    active: test
  datasource:
    url: jdbc:h2:mem:nuro-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  ai:
    model:
      chat: none
    google:
      genai:
        api-key: test-dummy-key
    anthropic:
      api-key: test-dummy-key

cloud:
  aws:
    credentials:
      access-key: test-dummy-key
      secret-key: test-dummy-secret
    region: ap-northeast-2
    s3:
      bucket: test-bucket

solapi:
  api-key: test-key
  api-secret: test-secret
  sender: "01000000000"
app:
  result-base-url: http://localhost:5173/result